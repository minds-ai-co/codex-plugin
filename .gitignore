# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Local plugin testing
.codex/
node_modules/

# Submission build artifacts (regenerable)
submission/build/
# NOTE: submission PDFs (one-pager.pdf, security-privacy.pdf) are committed
# deliverables per issue #9 (attached to the pitch email), so they are NOT ignored.
# Regenerate with: python3 submission/render-pdf helper — see PR #9 / submission notes.

# Secrets — never commit
.env
.env.local
*.key

# jumpcode workspace internals (never commit)
.jumpcode/
scratchpad/
