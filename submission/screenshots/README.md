# Screenshot capture targets

For the BD packet. Capture at 2560×1600 (Retina) or 1920×1200 minimum. Save as PNG.

| File | Content |
|---|---|
| `01-install.png` | Codex CLI window showing `codex plugin marketplace add minds-ai-co/codex-plugin` succeeding, then `codex plugin install minds` succeeding |
| `02-panel-query.png` | Codex session running the `query-panel` skill (e.g. `Ask my founders panel: Should we charge per-seat or per-usage?`) with the response visible — 6 personas, mixed answers, synthesis at the top |
| `03-results-synthesis.png` | Close-up of the synthesized answer block — quotes from 2 personas, alignment spread, links back to `getminds.ai/?sparkId=...` |

Use the `sean@getminds.ai` test account. No real customer data on screen.
